[project]
name = "note_models"
version = "0.1.0"
description = "Note content + version Pydantic models"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.7",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/note_models"]

[dependency-groups]
dev = [
    "pytest>=8",
    "hypothesis>=6.103",
]
