# Adversarial subsets (sprint 21 §8)

Each subdirectory holds utterances in the standard corpus layout
(`audio.wav` + `transcript.txt` + `metadata.json` with a `subset` field).
See `eval/corpus/v1/README.md` for the subset table and recording flow.
Empty until the clinician records; the directories exist so the frontend
recorder has stable drop targets.
